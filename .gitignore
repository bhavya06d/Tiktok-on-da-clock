.venv/
__pycache__/
*.pyc
.cache/
KuaiRand-Pure/
KuaiRand-Pure.tar.gz
submission.csv
.DS_Store
agent_log.jsonl
agent_summary.json
dashboard.html
runs_agent_log.jsonl
runs_agent_summary.json
runs_dashboard.html

# working-tree scratch is ignored (above), but the committed canonical run
# receipts under results/ are deliverables and must stay tracked
!results/
!results/**
# ...except this one: it's a live checkpoint rewritten every run (same info
# ends up in agent_summary.json), so keep it out of git to avoid churn
results/champion_checkpoint.json
